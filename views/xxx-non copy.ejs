<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Barbarians Table</title>
  <style>
    table { border-collapse: collapse; width: 100%; }
    th, td { border: 1px solid #ccc; padding: 8px; text-align: left; }
    th { background-color: #f2f2f2; }
  </style>
  <meta name="robots" content="noindex, nofollow">
</head>
<body>
  <h1>Barbarians Table</h1>
  <table>
    <thead>
      <tr>
        <% if (users.length > 0) { %>
          <% Object.keys(users[0]).forEach(col => { %>
            <th><%= col %></th>
          <% }) %>
        <% } %>
      </tr>
    </thead>
    <tbody>
      <% users.forEach(b => { %>
        <tr>
          <% Object.values(b).forEach(value => { %>
            <td><%= value %></td>
          <% }) %>
        </tr>
      <% }) %>
    </tbody>
  </table>
</body>
</html>
