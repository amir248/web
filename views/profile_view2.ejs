<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8">
  <title>Профиль пользователя</title>
  <style>
    body { font-family: Arial, sans-serif; background: #f4f4f4; }
    .profile { max-width: 400px; margin: 50px auto; background: #fff; padding: 20px; border-radius: 8px; }
    img.avatar { width: 150px; height: 150px; border-radius: 50%; object-fit: cover; }
  </style>
</head>
<body>
  <div class="profile">
    <h2>Профиль</h2>
    <p><strong>Логин:</strong> <%= user.login %></p>
    <p><strong>Email:</strong> <%= user.email %></p>
    <p><strong>Имя:</strong> <%= user.name || '-' %></p>
    <p><strong>Фамилия:</strong> <%= user.lastname || '-' %></p>
    <p><strong>Возраст:</strong> <%= user.age || '-' %></p>
    <p><strong>Пол:</strong> <%= user.gender || '-' %></p>
    <p><strong>Хобби:</strong> <%= user.hobby || '-' %></p>
    <p><strong>Аватар:</strong></p>
    <% if (user.avatar) { %>
      <img class="avatar" src="/<%= user.avatar %>" alt="Аватар">
    <% } else { %>
      <p>Аватар не загружен</p>
    <% } %>
    <br><br>
    <a href="/profile/edit">Редактировать профиль</a>
    <br>
    <a href="/logout">Выйти</a>
  </div>
</body>
</html>
